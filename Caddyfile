login.localhost {
        handle /api/* {
                reverse_proxy localhost:8088
        }
        handle /auth/* {
                reverse_proxy localhost:8088
        }
        handle {
                reverse_proxy localhost:3000
        }
}